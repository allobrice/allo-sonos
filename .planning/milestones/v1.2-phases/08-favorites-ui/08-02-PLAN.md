---
phase: 08-favorites-ui
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - frontend/src/components/ZoneCard.vue
autonomous: false
requirements: [FAV-02, NAV-01, NAV-02]

must_haves:
  truths:
    - "User sees a pink heart button on each online ZoneCard that opens the favorites panel"
    - "Tapping a favorite starts playback on that zone and closes the panel automatically"
    - "User can close the panel without selecting a favorite via the heart button, close button, or backdrop tap"
    - "Heart button is hidden on offline zones"
  artifacts:
    - path: "frontend/src/components/ZoneCard.vue"
      provides: "Heart button trigger + FavoritesSheet integration"
      contains: "FavoritesSheet"
  key_links:
    - from: "frontend/src/components/ZoneCard.vue"
      to: "frontend/src/components/FavoritesSheet.vue"
      via: "import and v-if mount"
      pattern: "FavoritesSheet"
    - from: "frontend/src/components/ZoneCard.vue"
      to: "frontend/src/stores/favorites.ts"
      via: "FavoritesSheet uses store internally"
      pattern: "showFavorites"
---

<objective>
Integrate the favorites heart button and FavoritesSheet into ZoneCard, completing the full user flow.

Purpose: Wire the trigger button, sheet open/close state, and play-favorite action into the existing ZoneCard so users can browse and launch favorites from any online zone.
Output: Modified `frontend/src/components/ZoneCard.vue`
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-favorites-ui/08-CONTEXT.md
@.planning/phases/08-favorites-ui/08-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From frontend/src/components/FavoritesSheet.vue (created in 08-01):
```typescript
// Props
defineProps<{
  visible: boolean
  zoneName: string
  zoneUuid: string
}>()

// Emits
defineEmits<{
  close: []
}>()
```

From frontend/src/components/ZoneCard.vue — current structure:
```html
<!-- zone-header contains zone-name + zone-source-icon (position: absolute top-right) -->
<div class="zone-header">
  <span class="zone-name">{{ zone.name }}</span>
  <span class="zone-source-icon"><!-- SVG icons --></span>
</div>
<!-- zone-body, transport-controls (v-if="!isOffline"), volume-row (v-if="!isOffline") -->
```

```css
.zone-source-icon {
  position: absolute;
  top: var(--space-sm);
  right: var(--space-sm);
  /* ... */
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add heart button and FavoritesSheet to ZoneCard</name>
  <files>frontend/src/components/ZoneCard.vue</files>
  <action>
Modify `frontend/src/components/ZoneCard.vue` to add the favorites heart button and mount the FavoritesSheet component.

**Import:**
Add at top of `<script setup>`:
```typescript
import FavoritesSheet from '@/components/FavoritesSheet.vue'
```

**State:**
Add after existing refs:
```typescript
const showFavorites = ref(false)
```

**Methods:**
Add after existing handlers:
```typescript
function toggleFavorites() {
  showFavorites.value = !showFavorites.value
}
```

**Template changes:**

1. **Heart button in zone-header** — Add a button BEFORE the `zone-source-icon` span, inside `.zone-header`. Only show when NOT offline (consistent with transport controls being hidden on offline zones per user decision):

```html
<button
  v-if="!isOffline"
  class="fav-btn"
  @click="toggleFavorites"
  aria-label="Favoris"
>
  <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
    <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
  </svg>
</button>
```

2. **FavoritesSheet at end of template** (just before closing `</div>` of `.zone-card`):

```html
<FavoritesSheet
  :visible="showFavorites"
  :zone-name="zone.name"
  :zone-uuid="zone.uuid"
  @close="showFavorites = false"
/>
```

**Style additions (scoped):**

`.fav-btn`: positioned in the header area. Since `.zone-source-icon` is `position: absolute; top: var(--space-sm); right: var(--space-sm)`, place the heart button to its LEFT. Use: position absolute, top `var(--space-sm)`, right `calc(var(--space-sm) + 22px)` (source icon is ~14px wide + some space). Background none, border none, cursor pointer, color `var(--color-accent-pink)`, padding `var(--space-xs)`, display flex, align-items center, justify-content center, min-width 28px, min-height 28px, -webkit-tap-highlight-color transparent, border-radius `var(--radius-sm)`. On `:active` — opacity 0.6. Transition `opacity 0.15s ease`.

This places the pink heart to the left of the source icon in the top-right corner of the card header, creating a clean icon pair.
  </action>
  <verify>
    <automated>cd C:/Users/Admin/WebstormProjects/allo-sonos/frontend && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - ZoneCard imports and mounts FavoritesSheet component
    - Pink heart button visible in header of online zone cards
    - Heart button hidden on offline zones
    - Clicking heart toggles showFavorites state
    - FavoritesSheet receives correct props (visible, zoneName, zoneUuid)
    - Sheet close event resets showFavorites to false
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete favorites flow</name>
  <what-built>
    Full favorites UI: pink heart button on ZoneCard header, bottom sheet with favorites list, type icons, loading skeleton, tap-to-play-and-close flow.
  </what-built>
  <how-to-verify>
    1. Start both backend and frontend dev servers
    2. Navigate to the zones dashboard — confirm each online zone card shows a pink heart icon in the top-right area
    3. Confirm offline zone cards do NOT show the heart icon
    4. Tap the heart on any online zone — confirm the bottom sheet slides up with "Favoris — {zone name}" header
    5. If this is the first load, confirm skeleton shimmer appears briefly before favorites load
    6. Confirm each favorite shows a type icon (radio/playlist/album) and title
    7. Tap any favorite — confirm the sheet closes immediately
    8. After a moment, confirm the zone card updates to reflect the new playback (via WebSocket)
    9. Re-open the sheet — confirm favorites load instantly (cached)
    10. Tap the backdrop (dark area outside sheet) — confirm sheet closes without playing anything
    11. Open the sheet and tap the X close button — confirm sheet closes
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx vue-tsc --noEmit` passes with no errors
2. Pink heart button visible on all online zone cards, hidden on offline
3. Bottom sheet opens with slide-up animation showing favorites
4. Tapping a favorite starts playback and closes the sheet
5. Sheet can be closed via heart button, X button, or backdrop click
6. Loading, error, and empty states render correctly
</verification>

<success_criteria>
- User can open favorites panel from any online zone card (NAV-01)
- User sees full favorites list with type icons (FAV-01, FAV-03)
- User can play a favorite and sheet auto-closes (FAV-02, NAV-02)
- User can close without selecting (NAV-01)
- Human verification confirms the complete flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/08-favorites-ui/08-02-SUMMARY.md`
</output>
