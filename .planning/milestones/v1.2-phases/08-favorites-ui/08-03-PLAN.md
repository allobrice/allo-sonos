---
phase: 08-favorites-ui
plan: "03"
type: execute
wave: 1
depends_on: ["08-01", "08-02"]
files_modified:
  - "backend/dist/"
  - "frontend/src/stores/favorites.ts"
autonomous: true
gap_closure: true
requirements: [FAV-02]

must_haves:
  truths:
    - "POST /api/speakers/:id/play-favorite returns 200 (not 404) when called"
    - "Tapping a favorite in FavoritesSheet starts playback on the target zone"
    - "If play-favorite API fails, a warning is logged to the console (not silently swallowed)"
  artifacts:
    - path: "backend/dist/routes/favorites.js"
      provides: "Compiled favorites route with play-favorite endpoint"
    - path: "backend/dist/services/sonos-favorites.js"
      provides: "Compiled favorites service for ContentDirectory SOAP"
    - path: "backend/dist/app.js"
      provides: "App entry that imports and registers favoritesRoutes"
      contains: "favorites"
    - path: "frontend/src/stores/favorites.ts"
      provides: "playFavorite with console.warn on error"
      contains: "console.warn"
  key_links:
    - from: "frontend/src/stores/favorites.ts"
      to: "POST /api/speakers/:id/play-favorite"
      via: "fetch in playFavorite"
      pattern: "api/speakers.*play-favorite"
    - from: "backend/dist/app.js"
      to: "backend/dist/routes/favorites.js"
      via: "import + register"
      pattern: "favorites"
---

<objective>
Fix the single UAT failure: tapping a favorite closes the sheet but does not start playback. The root cause is a stale backend dist/ build (compiled Mar 2) that predates the favorites feature (added Mar 3). The compiled dist/app.js does not import or register favoritesRoutes, and dist/routes/favorites.js does not exist. The frontend catch block silently swallows the resulting 404.

Purpose: Close the only remaining gap blocking v1.2 milestone shipment.
Output: Rebuilt backend dist/ with favorites routes; improved error visibility in frontend store.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-favorites-ui/08-UAT.md
@.planning/debug/favorite-not-playing.md

<interfaces>
<!-- From frontend/src/stores/favorites.ts (lines 42-54) — the playFavorite function to fix -->
```typescript
async function playFavorite(zoneUuid: string, favorite: Favorite) {
  // Fire-and-forget — WebSocket will push the new state
  try {
    await fetch(`/api/speakers/${zoneUuid}/play-favorite`, {
      method: 'POST',
      credentials: 'include',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ uri: favorite.uri, type: favorite.type }),
    })
  } catch {
    // Silently ignore — consistent with sendNext/sendPrevious pattern
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild backend dist to include favorites routes</name>
  <files>backend/dist/</files>
  <action>
Run `cd backend && npm run build` (which executes `tsc`) to recompile the TypeScript source into dist/.

After the build completes, verify that the following files now exist:
- `backend/dist/routes/favorites.js`
- `backend/dist/services/sonos-favorites.js`
- `backend/dist/app.js` contains the favorites import and registration

This is a build step, not a code change. The source code (backend/src/) is already correct from Phase 7. The dist/ directory was simply never recompiled after the favorites feature was added.

If the build produces TypeScript errors, fix them in the source files before retrying. Based on the debug session, the source compiles cleanly so no errors are expected.
  </action>
  <verify>
    <automated>cd backend && npm run build && node -e "const fs=require('fs'); const app=fs.readFileSync('dist/app.js','utf8'); if(!app.includes('favorites')) { console.error('FAIL: dist/app.js missing favorites import'); process.exit(1); } if(!fs.existsSync('dist/routes/favorites.js')) { console.error('FAIL: dist/routes/favorites.js missing'); process.exit(1); } if(!fs.existsSync('dist/services/sonos-favorites.js')) { console.error('FAIL: dist/services/sonos-favorites.js missing'); process.exit(1); } console.log('PASS: dist/ contains favorites routes and services');"</automated>
  </verify>
  <done>backend/dist/ is freshly compiled and contains favorites.js route, sonos-favorites.js service, and app.js with favorites import/registration</done>
</task>

<task type="auto">
  <name>Task 2: Add console.warn to playFavorite catch block</name>
  <files>frontend/src/stores/favorites.ts</files>
  <action>
In `frontend/src/stores/favorites.ts`, modify the `playFavorite` function's catch block (lines 51-53) to add a `console.warn` so that API failures are visible in the browser console during development and debugging.

Change from:
```typescript
  } catch {
    // Silently ignore — consistent with sendNext/sendPrevious pattern
  }
```

To:
```typescript
  } catch (err) {
    console.warn('[favorites] playFavorite failed:', err)
  }
```

This keeps the fire-and-forget pattern (no UI error state, no user-facing toast) but ensures failures are visible in the dev console. The silent catch was the reason the stale-dist 404 went undetected during development.

Also fix the `loadFavorites` catch block — it already sets `error.value` which is good, but the catch parameter should be named for consistency:

Change from (line 29):
```typescript
    } catch {
```

To:
```typescript
    } catch (err) {
```

This is a minor consistency improvement — the error message is already set via `error.value`, so no functional change needed there.
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit 2>&1 | head -20 && echo "TypeScript check passed"</automated>
  </verify>
  <done>playFavorite catch block logs a console.warn with error details; loadFavorites catch uses named parameter; TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. `backend/dist/routes/favorites.js` exists and is freshly compiled
2. `backend/dist/services/sonos-favorites.js` exists and is freshly compiled
3. `backend/dist/app.js` contains the favorites import
4. `frontend/src/stores/favorites.ts` has `console.warn` in playFavorite catch block
5. TypeScript compiles cleanly for both backend and frontend
</verification>

<success_criteria>
- POST /api/speakers/:id/play-favorite endpoint exists when running from dist/ (no more 404)
- Frontend playFavorite logs warnings to console on failure instead of silently swallowing
- UAT test 6 ("Jouer un favori") can pass: tapping a favorite closes the sheet AND starts playback
</success_criteria>

<output>
After completion, create `.planning/phases/08-favorites-ui/08-03-SUMMARY.md`
</output>
